{
    "_id" : "_design/category_subject",
    "views" : {
      "foo" : {
        "map" : "function(doc){ 
        	if (doc.subject) {
        		emit(doc.subject, 1)
        	}
        }"
      }
    }
}
